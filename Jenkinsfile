pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/Subbu07723/Subbus.git'
            }
        }

        stage('Build') {
            steps {
                echo 'Build started'
            }
        }
    }
}
